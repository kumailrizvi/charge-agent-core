const $=(q,el=document)=>el.querySelector(q); const $$=(q,el=document)=>[...el.querySelectorAll(q)]; const app=$('#app');
let STATE=null,page='landing',onStep=1,filters={},jobPage=1,selectedJob=null,selectedApp=null,workspaceTab='form',appTab='all',renderSeq=0,refreshTimer=null,uploadStatus='',uploadingResume=false;
async function api(path,opts={}){try{const r=await fetch(path,{headers:{'content-type':'application/json'},...opts});const t=await r.text();let j;try{j=JSON.parse(t)}catch{j={ok:false,raw:t}};if(!r.ok) return {...j,ok:false,error:j.error||('HTTP '+r.status)};return j}catch(e){return{ok:false,error:e.message||'Network error'}}}
async function load(){STATE=await api('/api/state'); if(STATE.user&&page==='landing') page=STATE.onboarding?.done?'dashboard':'onboarding'; render(); if(!refreshTimer)refreshTimer=setInterval(refresh,5000)}
async function refresh(){if(!STATE?.user)return; const old=page; STATE=await api('/api/state'); if(['dashboard','tracker','workspace','inbox'].includes(old)) render();}
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))} function decodeHtml(s){const t=document.createElement('textarea');t.innerHTML=String(s||'');return t.value} function toast(m){const t=document.createElement('div');t.className='toast';t.textContent=m;document.body.appendChild(t);setTimeout(()=>t.remove(),2800)}
function fmt(v){const d=new Date(v); return v&&!isNaN(d)?d.toLocaleString():esc(v||'—')} function initials(n){return String(n||'?').split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase()||'?'}
function icon(n){return {dash:'▦',search:'⌕',mail:'✉',tracker:'▤',user:'○',channels:'◇',settings:'⚙',bolt:'↯',file:'▣',check:'✓',ext:'↗'}[n]||'•'}
function nav(p){page=p;selectedJob=null;selectedApp=null;workspaceTab='form';render()}
function debounce(fn,ms=250){let t;return(...a)=>{clearTimeout(t);t=setTimeout(()=>fn(...a),ms)}}

function side(){const p=STATE?.profile||{}, name=p.name||STATE?.user?.name||'Applicant'; const links=[['dashboard','dash','Dashboard'],['browse','search','Browse jobs'],['inbox','mail','Inbox'],['tracker','tracker','Tracker'],['profile','user','Profile'],['accounts','channels','ATS accounts'],['channels','channels','Channels'],['settings','settings','Settings']]; return `<aside class="side"><div class="brand"><div class="bolt">${icon('bolt')}</div><span>Charge</span></div><nav>${links.map(([k,i,l])=>`<button class="side-link ${page===k?'active':''}" onclick="nav('${k}')"><span>${icon(i)}</span><span>${l}</span></button>`).join('')}</nav><div class="spacer"></div><div class="channel-grid"><button>iMessage</button><button>WhatsApp</button><button>Chrome</button><button>MCP</button></div><div class="credit"><b>${esc(name)}</b><span>Applications left</span><h1>${STATE?.credits??0}</h1><small>Free plan</small><button class="btn light full" onclick="nav('billing')">Upgrade</button><button class="btn ghost full" onclick="logout()">Log out</button></div></aside>`}
async function logout(){await api('/api/logout',{method:'POST'});STATE=null;page='landing';toast('Logged out');load()}
function shell(c){app.innerHTML=`<div class="app-shell">${side()}<main class="main">${c}</main></div>`}

function landing(){app.innerHTML=`<div class="landing product-landing"><section class="signup-shell"><div class="hero-grid"><div class="hero-blue"><div><div class="brand white"><div class="bolt">${icon('bolt')}</div><span>Charge</span></div><h1>Find. Prep. Apply. Track.</h1><p>Charge is your AI job application operator — it watches roles, prepares your packet, applies in the background, and routes replies back to you.</p><div class="pipeline-pills"><span>01 Find</span><span>02 Prep</span><span>03 Apply</span><span>04 Track</span></div></div></div><div class="signup-card"><div class="brand"><div class="bolt">${icon('bolt')}</div><span>Charge</span></div><h1>Create your account</h1><p class="muted">Upload your resume once. Charge builds your profile and starts matching jobs.</p><input id="suEmail" placeholder="Email"><input id="suName" placeholder="Full name" style="margin-top:12px"><input id="suPass" type="password" placeholder="Password" style="margin-top:12px"><label style="display:flex;gap:8px;margin:14px 0"><input type="checkbox" checked> I agree to the terms</label><button class="btn full" onclick="signupAccount()">Create account →</button><div class="mini-benefits"><p>✓ 50k-style career-page scanner architecture</p><p>✓ Resume/profile optimization before apply</p><p>✓ Web, Chrome, iMessage/WhatsApp, MCP endpoints</p></div></div></div><div class="how-grid"><div class="dark-card"><small>01 FIND → HEAD START</small><h2>The role finds you. You apply first.</h2><p>Async crawlers scan ATS sources, normalize openings, score matches, and notify your channels.</p></div><div class="panel"><small>02 PREP</small><h3>Every change is reviewable.</h3><p>Resume, cover letter, and application answers are generated from your parsed profile and the actual job description.</p></div><div class="panel"><small>03 APPLY</small><h3>Agent handles the form.</h3><p>The browser worker opens the real application page, fills fields, uploads the resume, and submits supported flows.</p></div><div class="panel"><small>04 TRACK</small><h3>Replies route back.</h3><p>Confirmation emails, OTPs, rejections, and interviews can be linked to the right application.</p></div></div></section></div>`}
async function signupAccount(){await api('/api/signup',{method:'POST',body:JSON.stringify({name:$('#suName').value,email:$('#suEmail').value,step:1,done:false})});onStep=1;page='onboarding';await load()}
function onboarding(){const p=STATE.profile||{}; const step=onStep; const pct=Math.round(step/6*100); const left=`<div class="signup-left"><h4>Resume</h4><p class="muted">${step===1?'Upload your resume. We parse it while you finish setup.':'Parsed. Finish setup and Charge will match jobs for you.'}</p><ul><li>✓ Pulls skills, roles and dates</li><li>✓ Drafts a first cover letter</li><li>✓ Builds an editable profile</li></ul><div style="height:160px"></div><small>Enter Continue<br>⇥ Tab Back</small></div>`; let body='';
 if(step===1) body=`<h1>Upload your resume.</h1><p class="muted">PDF, DOC, or TXT. Charge extracts your profile and creates your editable resume.</p><div class="drop" onclick="$('#resumeInput').click()"><div>☁<br><b>Drop your resume here, or browse</b><br><span class="muted">PDF, DOC, TXT • up to 25MB</span></div></div><input id="resumeInput" type="file" hidden onchange="uploadResume(true)"><div class="upload-state"><b>${esc(STATE.profile?.resumeFileName?'Stored resume':'Resume status')}</b><br><span>${esc(uploadStatus || (STATE.profile?.resumeFileName ? STATE.profile.resumeFileName : 'No resume stored yet. Choose a file above.'))}</span></div><input id="referral" placeholder="Referral code · optional"><button class="btn" onclick="nextOnboard()" ${uploadingResume?'disabled':''}>${STATE.profile?.resumeFileName?'Continue':'Skip / Continue'}</button>`;
 if(step===2) body=`<h1>Where do you live?</h1><p class="muted">Most job sites need a full address. We fill it automatically.</p><input id="address" placeholder="Address line" value="${esc(p.address||'')}"><div class="grid two"><input id="city" placeholder="City" value="${esc(p.city||'')}"><input id="postal" placeholder="ZIP / Postal Code" value="${esc(p.postalCode||'')}"></div><div class="grid two"><select id="country" onchange="render()"><option>${esc(p.country||'Canada')}</option><option>Canada</option><option>United States</option><option>United Kingdom</option><option>United Arab Emirates</option><option>Other</option></select><input id="state" placeholder="State / Province" value="${esc(p.state||'')}"></div><button class="btn" onclick="saveLocation()">Continue</button>`;
 if(step===3) body=`<h1>How should we reach out?</h1><p class="muted">Phone, email and LinkedIn show up on most applications.</p><input id="phone" placeholder="Phone with country code" value="${esc(p.phone||'')}"><input id="linkedin" placeholder="LinkedIn URL" value="${esc(p.linkedin||'')}"><input id="appEmail" placeholder="Email to use for applications" value="${esc(p.applicationEmail||p.email||'')}"><button class="btn" onclick="saveContact()">Continue</button>`;
 if(step===4) body=workStatusStep();
 if(step===5) body=`<h1>Set a password for sites that ask.</h1><p class="muted">Workday, iCIMS, Oracle and others sometimes require account creation. This is stored locally in this MVP.</p><div class="grid two"><input id="atsEmail" placeholder="Email to use" value="${esc(p.applicationEmail||p.email||'')}"><input id="atsPass" type="password" placeholder="Application password" value="${esc(p.defaults?.applicationPassword||'')}"></div><button class="btn light" onclick="generatePass()">Generate strong password</button><p class="muted">Encrypted storage is required before real production users.</p><button class="btn" onclick="savePasswordStep()">Save & continue</button>`;
 if(step===6) body=`<h1>How should we apply?</h1><div class="checklist"><div class="checkitem"><div><b>Resume optimization</b><p class="muted">Light tailoring: reorder and emphasize relevant experience.</p></div><select id="tailoring"><option value="off">Off</option><option value="light" selected>Light tailoring</option><option value="strong">Strong tailoring</option></select></div><div class="checkitem"><div><b>Auto-approve resume edits?</b><p class="muted">Skip preview and send optimized resume straight through.</p></div><div class="toggle-row"><button class="active" id="resumeAutoYes">Yes</button><button>No</button></div></div><div class="checkitem"><div><b>Cover letter optimization</b><p class="muted">Tailor tone and emphasis to each job.</p></div><select id="coverMode"><option value="off">Off</option><option value="light" selected>Light tailoring</option><option value="strong">Strong tailoring</option></select></div><div class="checkitem"><div><b>Auto-submit trusted ATS flows?</b><p class="muted">Greenhouse/Lever/Ashby can submit automatically. Hard ATS pauses for review.</p></div><label><input id="autoSubmit" type="checkbox"> Yes</label></div></div><button class="btn" onclick="finishOnboarding()">Finish setup</button>`;
 app.innerHTML=`<div class="landing"><section class="signup-shell"><div class="signup-top"><div class="progress"><span style="width:${pct}%"></span></div></div><div class="signup-body">${left}<div class="signup-step"><small>Step ${step} of 6</small>${body}</div></div></section></div>`}
async function uploadResume(silent=false){const input=$('#resumeInput')||$('#resumeFile'); const f=input?.files?.[0]; if(!f){uploadStatus='Choose a resume first.'; render(); return toast('Choose a resume first.')} if(f.size>25*1024*1024){uploadStatus='File is too large. Keep it under 25MB for this local MVP.'; render(); return toast(uploadStatus)} uploadingResume=true; uploadStatus='Uploading and storing '+f.name+'…'; render(); try{ const isText=/\.(txt|md|csv)$/i.test(f.name)||(f.type||'').startsWith('text/'); const dataUrl=await new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=()=>reject(new Error('Could not read file'));r.onload=()=>resolve(r.result);r.readAsDataURL(f)}); let text=''; if(isText){text=await f.text()} const result=await api('/api/resume',{method:'POST',body:JSON.stringify({fileName:f.name,fileBase64:dataUrl,text})}); if(!result.ok) throw new Error(result.error||'Upload failed'); uploadStatus=result.extracted?'Stored and parsed: '+result.fileName:'Stored: '+result.fileName+'. Supabase/local storage saved; parsing will run separately.'; uploadingResume=false; await load(); if(page==='onboarding'){page='onboarding';onStep=1;render()} if(!silent)toast(result.extracted?'Resume uploaded, stored, and parsed.':'Resume uploaded and stored.'); } catch(e){uploadingResume=false; uploadStatus='Upload failed: '+e.message; render(); toast(uploadStatus)} }
async function nextOnboard(){onStep=2;render()} async function saveLocation(){const profile={address:$('#address').value,city:$('#city').value,postalCode:$('#postal').value,country:$('#country').value,state:$('#state').value,location:[ $('#city').value,$('#state').value,$('#country').value].filter(Boolean).join(', ')}; await api('/api/profile',{method:'POST',body:JSON.stringify({profile,target:{countries:[profile.country],cities:[profile.city]}})}); onStep=3; await load()} async function saveContact(){await api('/api/profile',{method:'POST',body:JSON.stringify({profile:{phone:$('#phone').value,linkedin:$('#linkedin').value,applicationEmail:$('#appEmail').value}})});onStep=4;await load()}
function workStatusStep(){const c=(STATE.profile?.country||'Canada').toLowerCase(); let statuses=c.includes('united states')?['US Citizen','Permanent Resident','H-1B','F-1 Student','OPT','CPT','J-1','L-1','O-1','TN','E-3','Other']:c.includes('canada')?['Canadian Citizen','Permanent Resident','Open Work Permit','Closed Work Permit','PGWP','Study Permit','Visitor','Other']:c.includes('kingdom')?['UK Citizen','Indefinite Leave','Skilled Worker Visa','Graduate Visa','Student Visa','Other']:['Citizen','Permanent Resident','Work Permit','Student Visa','Other']; return `<h1>What's your work status?</h1><p class="muted">Questions change based on country: <b>${esc(STATE.profile?.country||'Canada')}</b></p><div class="option-grid">${statuses.map((s,i)=>`<button class="${i===1?'active':''}" onclick="$$('.option-grid button').forEach(b=>b.classList.remove('active'));this.classList.add('active')">${esc(s)}</button>`).join('')}</div><div class="checklist" style="margin-top:24px"><div class="checkitem"><span>Are you legally authorized to work in ${esc(STATE.profile?.country||'this country')}?</span><div class="toggle-row"><button class="active">Yes</button><button>No</button></div></div><div class="checkitem"><span>Will you now, or in the future, require sponsorship?</span><div class="toggle-row"><button>Yes</button><button class="active">No</button></div></div></div><button class="btn" onclick="saveWorkStatus()">Continue</button>`}
async function saveWorkStatus(){const active=$('.option-grid .active')?.textContent||''; await api('/api/profile',{method:'POST',body:JSON.stringify({profile:{defaults:{...STATE.profile.defaults,workAuthorization:'Yes — '+active,sponsorship:'No sponsorship'}}})});onStep=5;await load()}
function generatePass(){const p='Chg-'+Math.random().toString(36).slice(2,8)+'-'+cryptoRandom(); $('#atsPass').value=p} function cryptoRandom(){return Math.random().toString(36).slice(2,6).toUpperCase()}
async function savePasswordStep(){await api('/api/profile',{method:'POST',body:JSON.stringify({profile:{applicationEmail:$('#atsEmail').value,defaults:{...STATE.profile.defaults,applicationPassword:$('#atsPass').value}}})}); onStep=6; await load()}
async function finishOnboarding(){await api('/api/profile',{method:'POST',body:JSON.stringify({profile:{defaults:{...STATE.profile.defaults,tailoring:$('#tailoring').value,coverMode:$('#coverMode').value,autoSubmit:$('#autoSubmit').checked}}})}); await api('/api/signup',{method:'POST',body:JSON.stringify({name:STATE.profile.name,email:STATE.profile.email,roles:STATE.target.roles,countries:STATE.target.countries,cities:STATE.target.cities,step:6,done:true})});page='dashboard';await load()}

async function getJobs(limit=20){return api('/api/jobs?'+new URLSearchParams({page:jobPage,limit,...filters}).toString())}
function filterBar(){return `<div class="search-wrap"><span>${icon('search')}</span><input id="search" placeholder="Search by title, company, location…" value="${esc(filters.search||'')}"></div><div class="filters"><select id="role"><option value="">Role</option><option>Product</option><option>Manager</option><option>Engineering</option><option>Analyst</option></select><select id="workplace"><option value="">Workplace</option><option value="remote">Remote</option></select><input id="location" placeholder="Location" value="${esc(filters.location||'')}"><select id="ats"><option value="">ATS</option><option>greenhouse</option><option>lever</option><option>ashby</option><option>smartrecruiters</option><option>remoteok</option></select><button class="btn light" onclick="filters={};jobPage=1;render()">Clear</button></div>`}
function wireFilters(){const apply=debounce(()=>{filters.search=$('#search')?.value||'';filters.role=$('#role')?.value||'';filters.workplace=$('#workplace')?.value||'';filters.location=$('#location')?.value||'';filters.ats=$('#ats')?.value||'';jobPage=1;render()},250);['search','role','workplace','location','ats'].forEach(id=>{$('#'+id)?.addEventListener('input',apply);$('#'+id)?.addEventListener('change',apply);if($('#'+id))$('#'+id).value=filters[id]||''})}
function targetBanner(){const t=STATE.target||{};return `<div class="target-banner"><span><b>Target</b>: ${(t.roles||[]).join(', ')||'Product roles'} • ${(t.countries||[]).join(', ')||'Any country'} • ${(t.cities||[]).join(', ')||'Any city'}</span><button class="btn ghost" onclick="nav('settings')">Edit target</button></div>`}
function matchRing(s){return `<div class="match-ring"><b>${s||50}%</b><small>match</small></div>`} function jobCard(j){return `<article class="job-card"><div class="job-card-top"><span class="loc">${esc(j.location||'Remote')}</span>${matchRing(j.match?.score||50)}</div><h3>${esc(j.title)}</h3><div class="reason-row">${(j.match?.reasons||[]).slice(0,2).map(r=>`<span>${esc(r)}</span>`).join('')}</div><div class="job-card-bottom"><div><b>${esc(j.company)}</b><small>${esc(j.ats)}</small></div><div class="row"><button class="btn light" onclick='viewJob(${JSON.stringify(j.id)})'>View</button><button class="btn" onclick='applyJob(${JSON.stringify(j.id)})'>Apply</button></div></div></article>`}
async function dashboard(){const ticket=++renderSeq; const [jobs,apps]=await Promise.all([getJobs(20),api('/api/applications')]); if(ticket!==renderSeq)return; shell(`${targetBanner()}<div class="dash-search"><div>${filterBar()}</div><button class="btn green" onclick="applyTop()">Apply to all 5 →</button></div><div class="crawler-card"><b>Auto crawler running</b><span>${STATE.jobsCount||0} indexed jobs • scans automatically while server is running</span></div><h2 class="section-title">Top job matches</h2><div class="job-row">${jobs.items.slice(0,5).map(jobCard).join('')||'<div class="empty">Crawler is indexing jobs.</div>'}</div>${applicationsTable(apps.items||[])}`); wireFilters(); if(selectedJob) drawer(selectedJob)}
async function browse(){const ticket=++renderSeq; const jobs=await getJobs(21); if(ticket!==renderSeq)return; shell(`<div class="page-head"><h1>${jobs.total} openings</h1><button class="btn light" onclick="api('/api/crawl',{method:'POST'});toast('Crawler started')">Crawl all sources</button></div>${filterBar()}<div class="browse-grid" style="margin-top:22px">${jobs.items.map(jobCard).join('')}</div><div class="row" style="justify-content:center;margin:24px"><button class="btn light" onclick="jobPage=Math.max(1,jobPage-1);render()">Back</button><span>Page ${jobs.page}</span><button class="btn light" onclick="jobPage++;render()">Next</button></div>`); wireFilters()}
async function viewJob(id){const jobs=await getJobs(50);selectedJob=jobs.items.find(j=>j.id===id);render()} function drawer(j){document.body.insertAdjacentHTML('beforeend',`<aside class="drawer"><button class="x" onclick="this.closest('.drawer').remove()">×</button><h2>${esc(j.title)}</h2><p class="muted">${esc(j.company)} • ${esc(j.location)} • ${esc(j.ats)}</p>${matchRing(j.match?.score)}<div class="reason-row">${(j.match?.reasons||[]).map(r=>`<span>${esc(r)}</span>`).join('')}</div><p>${esc(j.description||'No description captured.').slice(0,2200)}</p><button class="btn full" onclick='applyJob(${JSON.stringify(j.id)})'>Apply with Charge</button></aside>`)}
async function applyJob(id){const res=await api('/api/apply',{method:'POST',body:JSON.stringify({jobId:id})});toast('Charge is applying in the background.');selectedApp=res.application;page='workspace';workspaceTab='form';await load()} async function applyTop(){const jobs=await getJobs(5);for(const j of jobs.items.slice(0,5)) await api('/api/apply',{method:'POST',body:JSON.stringify({jobId:j.id})});toast('Queued top 5 applications');await load()}
function statusClass(a){return a.status==='submitted'?'submitted':a.status==='in_flight'?'flight':a.status==='failed'?'failed':'review'}
function applicationsTable(items){const counts={all:items.length,submitted:items.filter(a=>a.status==='submitted').length,flight:items.filter(a=>a.status==='in_flight').length,review:items.filter(a=>a.status==='needs_review').length,failed:items.filter(a=>a.status==='failed').length,skipped:0};const shown=appTab==='all'?items:items.filter(a=>appTab==='flight'?a.status==='in_flight':a.status===appTab);return `<div class="page-head"><h2 class="section-title">All applications</h2><button class="btn light" onclick="nav('tracker')">Open tracker</button></div><div class="tabs">${[['all','All'],['submitted','Submitted'],['flight','In flight'],['review','Needs you'],['failed','Failed'],['skipped','Skipped']].map(([k,l])=>`<button class="${appTab===k?'active':''}" onclick="appTab='${k}';render()">${l} ${counts[k]}</button>`).join('')}</div><div class="app-table"><div class="app-head"><div>Company</div><div>Resume</div><div>Cover letter</div><div>Status</div><div>Applied</div><div>Action</div></div>${shown.length?shown.map(a=>`<div class="app-row"><div><b>${esc(a.company)}</b><br><span class="muted">${esc(a.role)}</span></div><div><span class="status ready">● Ready</span></div><div><span class="status ready">● Ready</span></div><div><span class="status ${statusClass(a)}">● ${esc(a.statusLabel)}</span><br><small class="muted">${esc(a.reason||'')}</small></div><div>${fmt(a.appliedAt)}</div><div class="row"><button class="btn" onclick='openApp(${JSON.stringify(a.id)})'>View profile</button>${a.status==='needs_review'?`<button class="btn green" onclick='approveApp(${JSON.stringify(a.id)})'>Approve</button>`:''}</div></div>`).join(''):'<div class="empty">No applications yet.</div>'}</div>`}
async function tracker(){const apps=await api('/api/applications');shell(applicationsTable(apps.items||[]))} async function openApp(id){selectedApp=await api('/api/applications/'+id);page='workspace';workspaceTab='form';render()} async function approveApp(id){await api(`/api/applications/${id}/approve`,{method:'POST'});toast('Approved. Charge is submitting in background.');await openApp(id)}
function progress(a){const status=a.status==='submitted'?'Completed':a.status==='in_flight'?'Submitting':'Ready for review';return `<div><span class="pill">● ${status}</span><div class="progress-steps">${['Optimize','Resume','Generate','Cover Letter','Submit','Done'].map((s,i)=>`<span class="dot ${a.status==='submitted'||i<4?'done':a.status==='in_flight'&&i===4?'live':''}">${i<5?'✓':''}</span>`).join('')}</div></div>`}
function workspace(){const a=selectedApp;if(!a)return nav('dashboard'); shell(`<div class="workspace-page"><aside class="workspace-rail"><div><b>${esc(a.company)}</b><br><span class="muted">${esc(a.role)}</span></div>${progress(a)}<div class="rail-nav"><button class="${workspaceTab==='form'?'active':''}" onclick="workspaceTab='form';render()">${icon('file')} Form</button><button class="${workspaceTab==='resume'?'active':''}" onclick="workspaceTab='resume';render()">${icon('file')} Resume</button><button class="${workspaceTab==='cover'?'active':''}" onclick="workspaceTab='cover';render()">${icon('mail')} Cover letter</button><button class="${workspaceTab==='jd'?'active':''}" onclick="workspaceTab='jd';render()">${icon('ext')} Job posting</button></div>${a.status==='needs_review'?`<button class="btn green full" onclick='approveApp(${JSON.stringify(a.id)})'>Approve & submit</button>`:''}<button class="btn light full" onclick="nav('dashboard')">Back</button></aside><section class="workspace-main">${workspaceTab==='form'?formTab(a):workspaceTab==='resume'?resumeTab(a):workspaceTab==='cover'?coverTab(a):jdTab(a)}</section></div>`)}
function formTab(a){const p=a.packet?.profileSnapshot||STATE.profile||{};const fields=a.formFields?.length?a.formFields:[];return `<div class="workspace-section"><div class="page-head"><h2>Application profile</h2><button class="btn green" onclick="saveApplicationForm('${a.id}')">Save form data</button></div><div class="form-grid"><div class="form-field"><label>Email</label><input id="appEmailField" value="${esc(p.applicationEmail||p.email||'')}"></div><div class="form-field"><label>Phone</label><input id="appPhoneField" value="${esc(p.phone||'')}"></div><div class="form-field"><label>LinkedIn</label><input id="appLinkedinField" value="${esc(p.linkedin||'')}"></div><div class="form-field"><label>Work authorization</label><input id="appAuthField" value="${esc(p.defaults?.workAuthorization||'')}"></div><div class="form-field"><label>Salary</label><input id="appSalaryField" value="${esc(p.defaults?.salary||'')}"></div></div><h3>Fields fetched from real application page</h3>${fields.length?fields.map((f,i)=>`<div class="form-field"><label>${esc(f.label||f.name||f.id||f.kind)}</label><input class="externalField" data-idx="${i}" value="${esc(f.value||'')}" placeholder="${esc(f.type||f.kind)}"></div>`).join(''):'<div class="empty">No external fields captured yet. Worker logs will show why.</div>'}<h3>Worker logs</h3><div class="logs">${(a.logs||[]).map(l=>`${fmt(l.at)} — ${esc(l.message)}`).join('<br>')}</div></div>`}
async function saveApplicationForm(id){const profileSnapshot={...(selectedApp.packet?.profileSnapshot||{}),applicationEmail:$('#appEmailField')?.value||'',phone:$('#appPhoneField')?.value||'',linkedin:$('#appLinkedinField')?.value||'',defaults:{...(selectedApp.packet?.profileSnapshot?.defaults||{}),workAuthorization:$('#appAuthField')?.value||'',salary:$('#appSalaryField')?.value||''}};const fields=$$('.externalField').map((el,i)=>({...((selectedApp.formFields||[])[i]||{}),value:el.value}));const res=await api(`/api/applications/${id}/packet`,{method:'POST',body:JSON.stringify({profileSnapshot,formFields:fields})});if(res.ok){selectedApp=res.application;toast('Saved. Approve/apply will use these values.')}else toast('Could not save form data.')}
function resumeTab(a){const text=a.packet?.resumeText||STATE.profile?.resumeText||'';return `<div class="doc-tools"><button class="btn light" onclick="toggleEdit('resumeDoc')">Toggle edit</button><button class="btn light" onclick="downloadText('resume.txt',$('#resumeDoc').innerText)">Download</button><button class="btn green" onclick="saveResumeFromDoc('${a.id}')">Save edits</button></div><p class="muted">Click into the resume, edit the text, then Save edits. Saved edits are used for this application.</p><div id="resumeDoc" contenteditable="true" class="doc-page editable editing">${resumeHtml(text)}</div>`}
function coverTab(a){const text=a.packet?.coverLetter||'';return `<div class="doc-tools"><button class="btn light" onclick="toggleEdit('coverDoc')">Toggle edit</button><button class="btn light" onclick="downloadText('cover-letter.txt',$('#coverDoc').innerText)">Download</button><button class="btn green" onclick="saveCoverFromDoc('${a.id}')">Save edits</button></div><div id="coverDoc" contenteditable="true" class="doc-page editable editing"><pre style="white-space:pre-wrap;font-family:Georgia,serif">${esc(text)}</pre></div>`}
function jdTab(a){return `<div class="workspace-section"><h2>${esc(a.role)}</h2><p class="muted">${esc(a.company)} • ${esc(a.ats)}</p><a class="btn light" target="_blank" href="${esc(a.url||'#')}">View job posting</a><div class="job-description-clean">${esc(decodeHtml(a.jobDescription||'No job description captured.')).slice(0,7000)}</div></div>`}
function resumeHtml(text){const parts=String(text||'').split(/\n+/);return `<h1>${esc(parts[0]||STATE.profile?.name||'Applicant')}</h1><p style="text-align:center">${esc(parts[1]||'')}</p>`+parts.slice(2).map(l=>/^([A-Z][A-Z\s]+)$/.test(l)?`<h2>${esc(l)}</h2>`:`<p>${esc(l)}</p>`).join('')} function toggleEdit(id){const el=$('#'+id);el.contentEditable=el.contentEditable!=='true';el.classList.toggle('editing');toast(el.contentEditable==='true'?'Editing on':'Editing off')} function downloadText(n,t){const b=new Blob([t],{type:'text/plain'});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=n;a.click();URL.revokeObjectURL(u)} async function saveResumeFromDoc(id){const el=$('#resumeDoc'); const res=await api(`/api/applications/${id}/packet`,{method:'POST',body:JSON.stringify({resumeText:el.innerText})}); if(res.ok){selectedApp=res.application;toast('Saved resume edits. This application will use the edited version.')}else toast('Could not save resume edits.')} async function saveCoverFromDoc(id){const el=$('#coverDoc'); const res=await api(`/api/applications/${id}/packet`,{method:'POST',body:JSON.stringify({coverLetter:el.innerText})}); if(res.ok){selectedApp=res.application;toast('Saved cover letter edits.')}else toast('Could not save cover letter edits.')} 
function profile(){const p=STATE.profile||{};shell(`<div class="profile-layout"><section><div class="profile-hero panel"><div class="avatar">${initials(p.name)}</div><div><h1>${esc(p.name||'Your profile')}</h1><p class="muted">${esc(p.summary||'Add a 1–2 line summary.')}</p><p>${esc(p.location||'')} ${p.email?' • '+esc(p.email):''} ${p.phone?' • '+esc(p.phone):''}</p></div><div class="completion"><b>${profileComplete()}%</b><span>complete</span></div></div><div class="profile-grid"><div class="panel"><h3>Resume</h3><p class="muted">Upload once. Charge parses, stores, and reuses it.</p><p><span class="pill">${p.resumeFileName?'Uploaded: '+esc(p.resumeFileName):'No resume uploaded'}</span></p><input type="file" id="resumeFile" onchange="uploadResume(false)"><p class="muted">${esc(uploadStatus||'Choose a file to store it in Charge.')}</p><button class="btn" onclick="uploadResume(false)">Upload & parse</button><button class="btn light" onclick="page='profileResume';render()">Preview & edit</button></div><div class="panel"><h3>Cover letter</h3><p class="muted">Default letter tailored per application.</p><button class="btn light">Preview & edit</button></div></div>${profileSection('Experience',p.experiences,'title','company')}${profileSection('Education',p.education,'school','degree')}<div class="profile-grid"><div class="panel"><h3>Skills <button class="btn light mini" onclick="editSkills()">Edit</button></h3><div class="tag-cloud">${(p.skills||[]).map(s=>`<span>${esc(s)}</span>`).join('')||'<span>No skills yet</span>'}</div></div><div class="panel"><h3>Certifications</h3><div class="empty">Add credentials, badges, or licenses.</div></div></div>${profileSection('Projects',p.projects,'name','description')}<div class="panel"><h3>Application defaults</h3><div class="tag-cloud"><span>${esc(p.applicationEmail||p.email||'No application email')}</span><span>${esc(p.defaults?.workAuthorization||'Work authorization not set')}</span><span>${esc(p.defaults?.sponsorship||'Sponsorship not set')}</span></div></div></section><aside class="profile-rail"><a class="active">Profile</a><a>Documents</a><a>Experience</a><a>Education</a><a>Skills</a><a>Certifications</a><a>Projects</a><a>Application defaults</a></aside></div>`)}
function profileSection(title,rows,a,b){return `<div class="panel profile-section"><h3>${title}<button class="btn light mini" onclick="editProfileJson('${title.toLowerCase()}')">Edit</button></h3>${rows?.length?rows.map(r=>`<div class="profile-row" data-initial="${esc(initials(r[b]||r[a]))}"><div></div><div><b>${esc(r[a]||'')}</b><br><span class="muted">${esc(r[b]||'')}</span><p>${esc(r.description||'')}</p></div></div>`).join(''):`<div class="empty">No ${title.toLowerCase()} added yet.</div>`}</div>`}
function profileComplete(){const p=STATE.profile||{};let n=0;['name','email','phone','location','resumeFileName'].forEach(k=>{if(p[k])n++});if((p.experiences||[]).length)n+=2;if((p.skills||[]).length)n++;return Math.min(100,Math.round(n/8*100))}
function profileResume(){shell(`<div class="page-head"><h1>My resume</h1><div><button class="btn light" onclick="nav('profile')">Back</button><button class="btn" onclick="toggleEdit('profileResumeDoc')">Toggle edit</button><button class="btn green" onclick="saveProfileResume()">Save edits</button></div></div><p class="muted">This is your reusable master resume. Edits here update future applications.</p><div id="profileResumeDoc" contenteditable="true" class="doc-page editable editing">${resumeHtml(STATE.profile?.resumeText?STATE.profile.resumeText:resumeFallback())}</div>`)} function resumeFallback(){return `${STATE.profile?.name||'Applicant'}\n${STATE.profile?.email||''}\n\nPROFESSIONAL SUMMARY\n${STATE.profile?.summary||''}\n\nSKILLS\n${(STATE.profile?.skills||[]).join(', ')}\n\nWORK EXPERIENCE\n${(STATE.profile?.experiences||[]).map(e=>`${e.company} | ${e.title}\n${e.description}`).join('\n\n')}\n\nEDUCATION\n${(STATE.profile?.education||[]).map(e=>`${e.school}\n${e.degree}`).join('\n')}`}
async function saveProfileResume(){const text=$('#profileResumeDoc').innerText; await api('/api/profile',{method:'POST',body:JSON.stringify({profile:{resumeText:text}})}); toast('Saved. Future applications will use this resume data.'); await load()}
function editSkills(){const v=prompt('Skills separated by commas',(STATE.profile.skills||[]).join(', '));if(v!=null)api('/api/profile',{method:'POST',body:JSON.stringify({profile:{skills:v.split(',').map(x=>x.trim()).filter(Boolean)}})}).then(load)} function editProfileJson(){alert('Inline section editor is next. Resume upload already parses these fields; Settings can edit defaults.')}
function accounts(){const p=STATE.profile||{}, accts=STATE.atsAccounts||[];shell(`<div class="page-head"><h1>ATS accounts</h1><button class="btn" onclick="saveAccount()">Save account</button></div><div class="panel password-card"><h3>Default account creation info</h3><div class="grid three"><input id="acctEmail" value="${esc(p.applicationEmail||p.email||'')}" placeholder="Application email"><input id="acctPass" type="password" value="${esc(p.defaults?.applicationPassword||'')}" placeholder="Password for sites"><select id="acctATS"><option>Workday</option><option>iCIMS</option><option>Oracle</option><option>Greenhouse</option><option>Lever</option></select></div><p class="muted">Charge uses this when an ATS asks to create an account mid-application.</p></div><div class="app-table" style="margin-top:18px"><div class="app-head"><div>Company / ATS</div><div>Email</div><div>Status</div><div>Created</div><div></div><div></div></div>${accts.map(a=>`<div class="app-row"><div><b>${esc(a.company||a.ats)}</b><br><span class="muted">${esc(a.ats)}</span></div><div>${esc(a.email)}</div><div><span class="status ready">${esc(a.status)}</span></div><div>${fmt(a.createdAt)}</div><div></div><div></div></div>`).join('')||'<div class="empty">No ATS accounts saved yet.</div>'}</div>`)} async function saveAccount(){await api('/api/accounts',{method:'POST',body:JSON.stringify({ats:$('#acctATS').value,email:$('#acctEmail').value,password:$('#acctPass').value,status:'saved'})});toast('ATS account saved locally.');await load()}
function inbox(){const msgs=STATE.messages||[];shell(`<div class="page-head"><h1>Inbox</h1><button class="btn light">Connect Gmail / Outlook</button></div><div class="panel"><div class="grid two"><input id="msgFrom" placeholder="From"><input id="msgSubject" placeholder="Subject"></div><textarea id="msgBody" placeholder="Paste OTP / confirmation email body"></textarea><button class="btn" onclick="addMessage()">Add message</button></div>${msgs.map(m=>`<div class="message"><b>${esc(m.category)} — ${esc(m.subject)}</b><p>${esc(m.snippet||m.body)}</p>${m.otp?`<span class="pill">OTP: ${esc(m.otp)}</span>`:''}</div>`).join('')||'<div class="empty">No messages yet.</div>'}`)} async function addMessage(){await api('/api/messages',{method:'POST',body:JSON.stringify({from:$('#msgFrom').value,subject:$('#msgSubject').value,body:$('#msgBody').value})});await load()}
function settings(){const p=STATE.profile||{},t=STATE.target||{};shell(`<h1>Settings</h1><div class="settings-grid"><div class="panel"><h3>Target</h3><input id="roles" value="${esc((t.roles||[]).join(', '))}" placeholder="Roles"><input id="countries" value="${esc((t.countries||[]).join(', '))}" placeholder="Countries"><input id="cities" value="${esc((t.cities||[]).join(', '))}" placeholder="Cities"></div><div class="panel"><h3>Application defaults</h3><input id="phone" value="${esc(p.phone||'')}" placeholder="Phone"><input id="linkedin" value="${esc(p.linkedin||'')}" placeholder="LinkedIn"><input id="appEmailSet" value="${esc(p.applicationEmail||p.email||'')}" placeholder="Application email"><input id="locationDefault" value="${esc(p.location||'')}" placeholder="Location"><select id="auth"><option>${esc(p.defaults?.workAuthorization||'Work authorization')}</option><option>Yes — Permanent Resident</option><option>Yes — Citizen</option><option>Yes — Work Permit</option></select><select id="sponsor"><option>${esc(p.defaults?.sponsorship||'Sponsorship')}</option><option>No sponsorship</option><option>Requires sponsorship</option></select><label><input type="checkbox" id="autoSubmit" ${p.defaults?.autoSubmit?'checked':''}> Auto-submit trusted ATS flows</label><button class="btn" onclick="saveSettings()">Save</button></div></div>`)} async function saveSettings(){const profile={...STATE.profile,phone:$('#phone').value,linkedin:$('#linkedin').value,applicationEmail:$('#appEmailSet').value,location:$('#locationDefault').value,defaults:{...STATE.profile.defaults,workAuthorization:$('#auth').value,sponsorship:$('#sponsor').value,autoSubmit:$('#autoSubmit').checked}};const target={roles:$('#roles').value.split(',').map(x=>x.trim()).filter(Boolean),countries:$('#countries').value.split(',').map(x=>x.trim()).filter(Boolean),cities:$('#cities').value.split(',').map(x=>x.trim()).filter(Boolean)};await api('/api/profile',{method:'POST',body:JSON.stringify({profile,target})});toast('Saved');await load()}
function channels(){shell(`<h1>Channels</h1><div class="settings-grid"><div class="panel"><h3>iMessage</h3><p>Endpoint scaffold: /api/channel/imessage</p></div><div class="panel"><h3>WhatsApp</h3><p>Twilio webhook: /webhooks/twilio/whatsapp</p></div><div class="panel"><h3>Chrome extension</h3><p>Use /extension folder.</p></div><div class="panel"><h3>MCP</h3><p>/api/mcp/manifest</p></div></div>`)} function billing(){shell(`<h1>Billing</h1><div class="settings-grid"><div class="panel"><h2>$19</h2><p>600 applications/month</p></div><div class="panel"><h2>$39</h2><p>1,500 applications/month</p></div><div class="panel"><h2>$99</h2><p>4,500 applications/month</p></div></div>`)}
function render(){if(!STATE)return landing(); if(!STATE.user&&page!=='onboarding')return landing(); if(page==='landing')return landing(); if(page==='onboarding')return onboarding(); if(page==='dashboard')return dashboard(); if(page==='browse')return browse(); if(page==='tracker')return tracker(); if(page==='workspace')return workspace(); if(page==='profile')return profile(); if(page==='profileResume')return profileResume(); if(page==='accounts')return accounts(); if(page==='inbox')return inbox(); if(page==='settings')return settings(); if(page==='channels')return channels(); if(page==='billing')return billing(); return dashboard()}
load();
